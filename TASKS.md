# Backlog після аудиту

## P0 — критичні баги та готовність до релізу

- [ ] Виправити `components/people/profile-card.tsx`: хук `useHydrated` синхронно викликає `setState` у `useLayoutEffect`. Через це `npm run lint` завершується з помилкою, отже CI/build зараз заблокований. Прибрати зайве проміжне hydrated-state або реалізувати SSR-safe синхронізацію без цього правила.
- [ ] Виправити зв'язок чатів і профілів у `lib/data.ts`. У `c4` вказано `personId: 3`, але ім'я/фото Ava (її профіль має id 4); у `c5` навпаки — Liam має id 3, але вказано 4. Посилання «View profile» ведуть до чужих профілів.
- [ ] Не підміняти неіснуючий чат першим записом: `ChatMessenger` для `/chat/<невідомий-id>` показує Sophie через `?? conversations[0]`. Потрібна сторінка 404/empty state і валідний пошук розмови за стабільним ID.
- [x] Виправити перехід «Message» з профілю: він формує URL з `user.id`, хоча маршрут чату очікує `conversation.id` (`c1`, `c2` тощо). Для більшості людей відкривається не той чат; за відсутності match чат не має відкриватися без створення розмови.
- [ ] Реалізувати реальну автентифікацію та авторизацію. Login/Register зараз лише переходять на `/discover`, пароль, email, вік і підтвердження пароля не перевіряються/не зберігаються, а всі `/ (app)` маршрути доступні без сесії.
- [ ] Підключити бекенд і завершити Prisma-модель. `schema.prisma` не містить моделей, у проєкті немає API/server actions і міграцій; усі дані (`lib/data.ts`) та критичні дії є demo-станом у браузері. Додати моделі User/Profile/Photo/Like/Match/Conversation/Message/Block/Report, серверну валідацію та контроль доступу.
- [ ] Реалізувати безпечні серверні операції для Like, Block, Report, Message, зміни пароля й видалення акаунта. Нинішні «успішні» повідомлення не виконують відповідних дій; delete/logout не очищають сесію та дані.
- [ ] Уніфікувати блокування: сторінка профілю пише в `localStorage` імена, Settings зберігає email/телефони; заблокований користувач не виключається з Discover, профілю та чату всупереч тексту інтерфейсу.

## P1 — функціональні та UX-покращення

- [ ] Застосувати filters до списку Discover. Поточні фільтри змінюють тільки значення в context/UI, але не впливають на `people`; також потрібні коректні числові поля віку, дистанції та семантика «Any distance».
- [ ] Зберігати профіль користувача, налаштування, likes і повідомлення на сервері. Зараз зміни профілю існують лише до оновлення сторінки, likes живуть окремо в localStorage, а filters взагалі не зберігаються.
- [ ] Зробити чат окремим для кожної розмови: усі діалоги отримують однаковий `seedMessages`, нові повідомлення пропадають при переході/перезавантаженні, лічильники та preview не оновлюються.
- [ ] Додати валідацію й error state під час завантаження вкладень: у чаті відсутні перевірки MIME/розміру, в реєстрації «додати фото» підставляє demo-зображення замість file upload. На бекенді потрібні дозволений тип, ліміт, антивірусна перевірка та безпечне сховище.
- [ ] Звільняти object URL (`URL.revokeObjectURL`) після видалення/заміни фото та при unmount у профілі й чаті, щоб уникати витоків пам'яті.
- [ ] Замінити клікабельний `span` у `LikedCard` на `button` з коректним aria-label; перевірити інші icon-only controls на доступну назву, keyboard focus і Escape/focus trap у модалках.
- [ ] Зробити інтерактивними або прибрати посилання-заглушки: Forgot password і всі пункти футера ведуть на `#`.
- [ ] Замінити `alert()` у керуванні фото на inline validation/toast і додати повідомлення про стан завантаження.
- [ ] Не маркувати всі сповіщення прочитаними при натисканні на дзвіночок; читання має відбуватися окремо або при перегляді конкретного повідомлення, із серверним станом.

## P2 — якість коду, продуктивність і тестування

- [ ] Усунути всі lint warnings: невикористані `useEffect` у profile/likes context, `Select` у settings, `onBlockUser` у TabPanel; після цього увімкнути lint як обов'язкову CI-перевірку.
- [ ] Замінити звичайні `<img>` на `next/image` там, де зображення статичні, додати `sizes`, розміри й placeholder. ESLint знаходить такі випадки в профілях, чаті, картках, hero та auth layout; для user uploads налаштувати image loader/remote patterns.
- [ ] Додати unit-тести для мапінгу conversation/profile, filters, likes і validation, а також E2E для login/register, захисту маршрутів, блокування, відкриття чату та видалення акаунта.
- [ ] Упорядкувати доменну модель і типи: `Settings` у `currentUser` має бути `settings`, а ID користувачів/чатів — стабільними типізованими ідентифікаторами, не змішаними з display name/email.
- [ ] Винести дубльовану логіку фільтрів і блокування зі сторінок у єдиний сервіс/store; додати optimistic updates з rollback та помилки мережі.
- [ ] Замість таймерів-імітацій loading/deleting використати фактичні pending/error/success стани запитів, а таймери toast коректно очищати при unmount.
- [ ] Додати privacy/safety-функції, необхідні для dating app: 18+ age gate, верифікація email, rate limits/anti-spam, модерація report, аудит небезпечних дій і політики Privacy/Terms/Cookies.

## Результати перевірок

- `npx tsc --noEmit` — проходить.
- `npm run lint` — не проходить: 1 error і 15 warnings; перша задача P0 описує блокуючу помилку.
- Автоматичних тестів у репозиторії немає.
